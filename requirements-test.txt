# Testing framework dependencies
pytest>=7.0.0
pytest-json-report>=1.5.0
pytest-cov>=4.0.0
faker>=18.0.0
pandas>=1.5.0
numpy>=1.21.0

# Data validation
jsonschema>=4.0.0

# Optional: For enhanced test reporting
pytest-html>=3.1.0
pytest-xdist>=3.0.0  # For parallel test execution

# Note: unittest.mock is part of Python's standard library (Python 3.3+)
# No need to install it separately
